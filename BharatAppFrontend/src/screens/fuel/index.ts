/**
 * Fuel department surface — the FUEL_MANAGER experience.
 * Owns the FuelDashboard and (later) this department's manage screens.
 */
export {default as FuelDashboardScreen} from './FuelDashboardScreen';
