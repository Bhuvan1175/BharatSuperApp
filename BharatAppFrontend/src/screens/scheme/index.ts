/**
 * Scheme department surface — the SCHEME_MANAGER experience.
 * Owns the SchemeDashboard and (later) this department's manage screens.
 */
export {default as SchemeDashboardScreen} from './SchemeDashboardScreen';
