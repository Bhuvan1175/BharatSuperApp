/**
 * Citizen surface — the PUBLIC_USER experience.
 *
 * The citizen "dashboard" is the existing 5-tab app (Home / Explore / Travel /
 * Government / Profile), so RoleRouter maps CitizenDashboard → BottomTabNavigator
 * and no screen is duplicated here. This folder hosts future citizen-only
 * surfaces.
 */
export {};
