/**
 * Water department surface — the WATER_MANAGER experience.
 * Owns the WaterDashboard and (later) this department's manage screens.
 */
export {default as WaterDashboardScreen} from './WaterDashboardScreen';
