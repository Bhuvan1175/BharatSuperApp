/**
 * Area department surface — the AREA_MANAGER experience.
 * Owns the AreaDashboard and (later) this department's manage screens.
 */
export {default as AreaDashboardScreen} from './AreaDashboardScreen';
